services:
  - type: web
    name: bias-desk-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FINNHUB_API_KEY
        sync: false   # Render will prompt you to enter this in the dashboard — it never goes in the repo
